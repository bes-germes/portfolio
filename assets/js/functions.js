function sortBy(a = 0) {

    switch (a) {
        case 0:
          alert( 'Маловато' );
          break;
        case 1:
          alert( 'В точку!' );
          break;
        case 2:
          alert( 'Перебор' );
          break;
        default:
          alert( "Нет таких значений" );
      }
}